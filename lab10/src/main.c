
#include <stdio.h>
#include <stdlib.h>

#include "lib.h"

int main()
{
	int first_number = 100;
	int second_number = 50;
	//int multiply[15][15];

	//multiply_matrix(argc, argv, (int *)multiply);
	printf(" first number = %d\n second number = %d\n", sum(10, 12),
	       mul(10, 12));

	return 0;
}
